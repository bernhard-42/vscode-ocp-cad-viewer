# Python

## Reliable Python detection

*OCP CAD Viewer* depends on VS Code using the correct Python interpreter. The most reliable way to use the *OCP CAD Viewer* is to start VS Code from the command line in the desired Python environment, or to select the correct Python interpreter in VS Code first.

